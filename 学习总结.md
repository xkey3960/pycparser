---
tags: [pycparser, C语言, 解析器, 解释器, 学习笔记]
created: 2026-01-01
---

# pycparser 学习总结（19.work-space/pycparser）

> 本仓库是 **eliben/pycparser** 的个人 fork，定位：**学习 C 语法 + 实现一个与 GCC 兼容的 C 解析器**，并在此基础上扩展出一个 C AST 解释器。

## 一、项目是什么

| 项目 | 说明 |
|------|------|
| 上游 | [eliben/pycparser](https://github.com/eliben/pycparser)：纯 Python 写的 C 语言解析器，无外部依赖，BSD-3-Clause |
| 本仓库版本 | v3.00，跟踪上游 `master`（已合并到 release 3.00 附近） |
| 语言标准 | 完整 C99（ISO/IEC 9899），部分 C11；目标是扩展 GCC 扩展语法 |
| 用途 | 任何需要解析 C 代码的场景；最著名的使用者是 cffi |
| 分支策略 | `master` 同步上游；自定义工作在 `lab/*` 分支（当前：`lab/learning`） |
| 远程 | `git@192.168.124.2:/srv/pycparser.git`（自建 git 服务器） |

## 二、架构图与系统框图（Excalidraw）

> 这两张图由可复用技能 `.agents/skills/excalidraw-software-diagram/` 声明式生成（spec 见其 `examples/` 目录），改文案后执行 `node .agents/skills/excalidraw-software-diagram/scripts/make.js <spec>` 即可重新生成。

![[pycparser-架构图.excalidraw|1100]]

![[pycparser-系统框图.excalidraw|1100]]

## 三、目录结构总览

```
pycparser/
├── pycparser/            # 上游核心包（未改动）
│   ├── c_parser.py       # 解析器主类 CParser（约 92KB，核心）
│   ├── c_lexer.py        # 词法分析器（PLY 风格正则）
│   ├── c_ast.py          # AST 节点定义（由 _c_ast.cfg 自动生成）
│   ├── _c_ast.cfg        # AST 节点配置（修改 AST 需重新生成）
│   ├── _ast_gen.py       # AST 代码生成器
│   ├── c_generator.py    # AST → C 源码（反解析）
│   └── ast_transforms.py # 常见 AST 变换（如修正函数指针声明）
├── pycparserext/         # ★ 自定义：GNU C 扩展解析器
├── examples/             # 上游示例（dump_ast / cdecl / c_json 等）
├── tests/                # 上游单元测试
├── utils/
│   ├── fake_libc_include/  # 假的标准库头文件（解析必须）
│   ├── benchmark/          # 性能测试（redis/sqlite 预处理产物）
│   └── internal/           # 内部工具
├── execute.py            # ★ 自定义：C AST 解释器（2001 行）
├── test_exe.py           # ★ 自定义：解析+执行端到端测试
├── test_ext.c            # ★ 自定义：GCC 扩展语法测试用例
├── test_ext.py           # ★ 自定义：扩展解析器 + 函数收集测试
├── test/main.c           # ★ 自定义：struct/typedef 测试用例
├── TODO.txt              # ★ 自定义：解释器开发路线图
├── README.rst            # 【已修改】说明本 fork 的目的
└── LICENSE / pyproject.toml / Makefile 等
```

## 四、自定义工作（lab/learning 分支，重点）

### 1. GNU C 扩展解析器 — `pycparserext/`

继承上游 `CParser`，扩展出 GCC/OpenCL 语法支持：

- **AST 扩展节点（11 个）**：`TypeList`、`AttributeSpecifier`（`__attribute__`）、`Asm`（内联汇编）、`PreprocessorLine`、`TypeOfDeclaration` / `TypeOfExpression`（`__typeof__`）、`RangeExpression`（`a...b`）、`TypeDeclExt`、`ArrayDeclExt`、`StructExt`、`FuncDeclExt`
- **解析器类**：`GnuCParser`、`OpenCLCParser`（基于 `CParserBase` + `_AsmAndAttributesMixin`）
- **已支持的扩展语法**：
  - `__attribute__((aligned(8)))` 等属性说明
  - `__asm__` 内联汇编
  - `__typeof__(x)` 类型推导
  - **GNU 语句表达式** `({ i; })` 作为表达式使用（本分支最早的自定义成果之一）

### 2. C AST 解释器 — `execute.py`

用 Python 直接"执行" C 代码的 AST，**共覆盖 60 个节点类型 = 49 个标准节点 + 11 个 GNU 扩展节点**。

- **分发机制**：`g_exe_class` 字典（节点类名 → Exe 执行器类），`execute(node)` 查表分发
- **运行时**：
  - `Scope`：父链作用域（块级作用域），`get/set/declare`
  - `Function`：用户函数（参数、函数体、闭包作用域）
  - 控制流异常：`ReturnException` / `BreakException` / `ContinueException`
  - 全局状态：`g_scope`、`g_functions`
- **执行器类**：`ExeConstant` / `ExeBinaryOp` / `ExeUnaryOp` / `ExeAssignment` / `ExeFuncCall` / `ExeCompound` / `ExeIf` / `ExeWhile` / `ExeFor` / `ExeSwitch` / `ExeStruct` / `ExeEnum` …… 及全部 GNU 扩展节点
- **内置测试**：约 30 个，覆盖表达式、赋值、运算符、控制流、函数、struct/union/enum、指针自增自减、GNU 扩展节点等

### 3. 测试与用例

| 文件 | 内容 |
|------|------|
| `test_exe.py` | 用 `GnuCParser` 解析 `test/main.c`，附加调用 `main()` 后执行，打印作用域 |
| `test_ext.py` | `CustomerCParser`（重载 `_build_function_definition` 收集函数表 `func_db`）+ AST 遍历访问器 |
| `test_ext.c` | GCC 扩展语法样例：`__attribute__`、函数指针、语句表达式 |
| `test/main.c` | typedef + struct（`TmpStruct_S`）用例 |

### 4. 开发路线图 — `TODO.txt`

解释器已知问题与改进计划，按优先级分类：

- **P0 紧急**：`sizeof` / `__alignof__` 不应求值 operand（编译期语义）；`_Static_assert` 时机
- **P1 内存模型**（第 1-2 周）：`Address`/`Memory` 指针模型（`&` `*` 指针算术、NULL、函数指针）、`CallStack` 栈帧与递归、`StructLayout` 布局与对齐、`Heap` 与 malloc/free
- **P2 类型系统**（第 2-3 周）：`CType` 类层次、隐式类型转换（Usual Arithmetic Conversions）、常量折叠
- **P3 控制流**（第 4 周）：Goto/标签、setjmp/longjmp、可变参数函数
- **P4 质量**：自定义异常与行号信息、测试覆盖率、性能优化（缓存分发、避免异常控制流）

## 五、分支与提交历史（lab/learning，按时间从早到晚）

```
5fd7768 Merge branch 'master' into lab/learning   ← 合并上游
943c07e support compound-statement in expression  ← GNU 语句表达式
43cd699 add pycparserext & fix asm / ({i;})==1    ← 扩展解析器
622be0b support function list
30096ae 一个类里，遍历多种节点                    ← 执行器雏形
40b63c6 create execute.py                         ← 解释器诞生
f0ff8cd / 79aa0c1 / f189bb0                       ← 修复 Assignment 的 Symbol 问题
e548e93 补齐所有 AST 节点类型的执行逻辑及测试      ← 49 个标准节点
48659f3 补齐 GNU C 扩展 AST 节点的执行器与测试    ← 11 个扩展节点
1477dde docs: 添加 execute.py 解释器开发路线图     ← TODO.txt
bb08de4 增加测试：解析c代码+执行c代码
91681d2 / 30ac1e6                                 ← p++/p-- 修复 + 单测
3ba580b add strcut test & print scope             ← HEAD
```

## 六、如何使用

```bash
# 解析 C 代码（需要先预处理）
python -c "
from pycparser import parse_file
ast = parse_file('examples/c_files/basic.c', use_cpp=True,
                 cpp_args=r'-Iutils/fake_libc_include')
ast.show()
"

# 解析 GCC 扩展语法（GnuCParser）
# 见 test_ext.py / test_exe.py：用 gcc -E 预处理 + GnuCParser 解析 + execute 执行
```

注意：真实 C 代码几乎都必须先经过 C 预处理（`cpp` / `gcc -E`），并用
`utils/fake_libc_include` 假头文件替代真实标准库头。

## 七、当前状态与下一步

- ✅ 完成：GCC 扩展解析（attribute/asm/typeof/语句表达式）、60 节点解释器、端到端解析+执行测试
- 🔜 进行中：指针/内存模型、递归栈帧、struct 布局（TODO 阶段二）
- 📌 边界：README 明确声明"实验项目，勿用于生产"

---

*整理时间：2026-01* ｜ *基于仓库 `lab/learning` 分支 HEAD `3ba580b`*
