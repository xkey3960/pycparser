---
tags: [pycparser, execute.py, 设计文档, 类型系统]
created: 2026-08-20
---

# 设计文档：常量折叠（TYPE-3）

> 问题：编译期常量表达式（`case 1+2:`、位域宽度 `int x: 4+4`）目前**运行时自然
> 求值**（execute 逐节点执行），未统一走常量折叠；数组维度/enum 已用
> `_eval_constant`。
> 现状基线：lab/learning `e1ae963`（TYPE-2 隐式类型转换已完成）

---

## 一、现状：哪些场景已折叠，哪些没有

| 场景 | 现状 | 说明 |
|---|---|---|
| 数组维度 `int arr[10+5]` | ✅ `_eval_constant`（typesys） | M5 修复 |
| enum 初始值 `A=0x100` | ✅ execute 求值（ExeConstant 进制解析） | 60f03b0 |
| case 标签 `case 1+2:` | ⚠️ `execute(case_expr)` 运行时求值 | 功能可用，但非编译期语义 |
| 位域宽度 `int x: 4+4` | ❌ bitsize 原样传递，不折叠 | `struct` 布局时未验证 |
| `_Static_assert` cond | ⚠️ 运行时求值（BUG-3 关联） | 归 BUG-3 |

**TYPE-3 目标**：统一 `_eval_constant` 为折叠入口，接入 case 标签与位域宽度。

## 二、设计：折叠入口统一

### 2.1 `_eval_constant`（typesys.py，已有）

```python
def _eval_constant(node):
    """常量表达式求值（编译期上下文）。
    Constant → C 字面量解析；否则延迟导入 execute 求值（sizeof/算术等）。
    求值失败/非常量 → None。"""
```

能力已覆盖：字面量（10/16/8/2 进制 + U/L 后缀）、BinaryOp 算术、sizeof、
引用枚举常量（execute 路径）。**无需改动本体**，只需在缺失场景调用它。

### 2.2 case 标签折叠（ExeSwitch）

```python
case_val = _eval_constant(case_expr)  # 替换 execute(case_expr)
if case_val == cond: matched = True
# RangeExpression 同理：_eval_constant(first/last)
```

- `case 1+2:` → 折叠为 3（编译期）
- 引用枚举常量 `case RED:` → execute 路径解析（RED 在 g_scope）——`_eval_constant`
  内部经 execute 求值，兼容
- 不可折叠（依赖运行变量）→ `_eval_constant` 返回 None → 回退 execute（宽松）

### 2.3 位域宽度折叠（struct 布局）

`_register_compound`/`ensure_complete` 布局成员时，bitsize 若是**表达式**而非
常量（pycparser 通常已把 `int x: 4` 解析为 Constant，但 `int x: 4+4` 是 BinaryOp）：

```python
bs = d.bitsize
if isinstance(bs, c_ast.Constant):
    bitsize = int(bs.value)          # 直接
else:
    bitsize = _eval_constant(bs)     # 折叠表达式
    if bitsize is None:
        bitsize = None               # 非常量 → 按普通成员（宽松）
```

## 三、对现有功能的影响

| 现有能力 | 变化 |
|---|---|
| `case 1+2:` | 行为不变（execute 也能算），语义改为编译期折叠 |
| 引用运行变量的 case | `_eval_constant` 失败 → 回退 execute（保持可用） |
| 数组维度/enum | 不变（已走 _eval_constant） |
| 位域 `int x: 4` | 不变（Constant 直接解析）；`int x: 4+4` 新增折叠 |
| switch 默认分支 | 不变 |

## 四、实施步骤

1. `ExeSwitch`：case 表达式与 RangeExpression 用 `_eval_constant`（失败回退 execute）
2. `typesys` 布局（`_register_compound`/`ensure_complete`）：bitsize 折叠
3. 测试：
   - `case 1+2:` / `case RED:`（枚举常量）/ `case 'A':`（字符）
   - 位域 `int x: 4+4`（折叠为 8，布局正确）
   - 数组维度 `int arr[1+2]`（回归）
4. 全量回归 + 文档更新

## 五、验收

- ✅ `switch` 中 `case 1+2:` 匹配 `3`
- ✅ `case RED:`（枚举）匹配枚举值
- ✅ 位域 `int x: 4+4` 宽度 8、布局正确
- ✅ 既有测试零回归（case 数值、enum、数组维度）

---

*基线：lab/learning `e1ae963`* ｜ *关联：TODO.txt TYPE-3、设计文档-类型系统支持.md（位域 M3）、execute.py ExeSwitch*
