---
tags: [pycparser, execute.py, 设计文档, 内置函数, cbuiltins]
created: 2026-08-15
---

# 设计文档：内置函数（cbuiltins 注册表）

> 目标：让解释器支持常用内置函数（printf/malloc/memset 等），并提供**易扩展的注册机制**。
> 现状基线：lab/learning `4f4bf90`（类型系统 M0-M5 + 多文件 S1-S3 + 修复）

---

## 一、现状与问题

修改前 `ExeFuncCall._call_builtin` 是**一长串 `if name == ...` 硬编码**（printf/putchar/getchar/exit/abs/`__builtin_types_compatible_p`），新增一个内置函数就要改分发函数，且功能零散。

## 二、注册机制（核心设计）

新增 `cbuiltins.py`，内置函数注册表 + 装饰器：

```python
# cbuiltins.py
_BUILTINS = {}                          # name -> handler(args) -> 值

def register_builtin(name, handler):    # 命令式注册
    _BUILTINS[name] = handler

def builtin(name):                      # 装饰器注册（推荐）
    def deco(f):
        register_builtin(name, f)
        return f
    return deco

def call_builtin(name, args):           # 分发；未注册 → AssertionError
    ...

def list_builtins():                    # 列出全部（调试/文档）
    ...
```

`execute.py` 只做两件事：`import cbuiltins`（触发注册副作用）+ `_call_builtin` 改为一行委托：

```python
def _call_builtin(self, name, args):
    return call_builtin(name, args)     # 经注册表分发，无需再改分发逻辑
```

## 三、如何新增内置函数（三步）

1. **定义 handler**：在 `cbuiltins.py` 用 `@builtin('函数名')` 装饰，`handler(args)` 接收**已求值的实参列表**（Python 值），返回 C 语义的结果：

```python
@builtin('mypow')                        # ① 装饰器注册
def _mypow(args):
    base, exp = args[0], args[1]
    return base ** exp                    # ② 实现逻辑
```

2. **需要状态**：用模块级变量（如 rand 的种子 `_RAND_STATE`，用 `global` 修改）。
3. **完成**：无需改 execute.py 分发；`call_builtin('mypow', [2, 10])` 即可调用，C 代码里 `mypow(2, 10)` 自动经 `ExeFuncCall → _call_builtin` 分发。

> 若要按文件组织更多内置函数，可拆多个模块：每个模块 import 后 `register_builtin`（或 `@builtin` 装饰在模块加载时执行），在主入口统一 `import` 即可。

## 四、简易堆模型（malloc/free 基础）

malloc 系列基于一个 **1MB 字节堆**（`bytearray`，bump 分配器）：

| 函数 | 语义（v1） |
|---|---|
| `malloc(n)` | 分配 n 字节，返回堆内偏移（int 地址）；越界报错 |
| `calloc(n, sz)` | 分配 n*sz（堆初始全零） |
| `realloc(p, n)` | 收缩返回同址；扩展则拷贝旧数据到新址 |
| `free(p)` | bump 分配器不回收空间（**MEM-4 first-fit** 后再做），仅移除分配记录 |
| `memset/memcpy/memmove/memcmp` | 堆内读写，**带越界检查** |
| `strlen/strcmp/strcpy/strcat` | 参数为 str 或堆地址均可（地址 → 读 NUL 结尾串） |

**限制**：逐字节解引用（`*ptr`、`p[i]`）依赖指针模型 **MEM-1**；堆回收依赖 **MEM-4**。当前堆是"写得进去、读得出来"的字节存储，为指针模型落地打基础。

## 五、printf 格式转换

C 的 `%d %s %c %x %f %u` 与 Python `%` 基本一致；差异处理（`_pyfmt`）：

- 长度修饰符剥离：`%ld` `%lu` `%llu` `%zu` `%zd` `%lf` → Python 对应转换符
- `%p` → `%#x`（十六进制指针）
- `%%` 原样保留

## 六、顺带修复：字面量解析（真实 bug）

内置函数调试暴露 pycparser 的两个集成问题，已在 `ExeConstant` 修复：

| 问题 | 现象 | 修复 |
|---|---|---|
| 字符串 value 自带引号 | `"hello"` → `'"hello"'`（len=7） | `_string_literal_value` 剥引号 |
| 字符串转义未解 | `"len=%d\n"` 里 `\n` 是两字符 | `_unescape_c_string` 解 `\n \t \xHH \0` 等 |
| 字符 value 自带引号 | `'A'` → `ord("'")=39`（错） | `_char_literal_code` 剥引号 + 转义（`'\n'`→10） |

这使 `strlen("hello")=5`、`memset(p,'A',3)` 写 65 等语义正确。

## 七、内置函数清单（47 个）

| 分类 | 函数 |
|---|---|
| stdio | printf puts putchar getchar exit _exit |
| stdlib | malloc calloc realloc free abs atoi atol rand srand |
| string | memset memcpy memmove memcmp strlen strcmp strncmp strcpy strncpy strcat |
| ctype | isdigit isalpha isalnum isspace isupper islower toupper tolower |
| math | sqrt pow fabs floor ceil fmod sin cos tan log exp |
| GNU | __builtin_types_compatible_p |

## 七·补、可变参数支持（__builtin_va_list）

**类型层**（typesys 内建注册，不依赖 fake_libc）：
- `__builtin_va_list` / `__gnuc_va_list`：8B 句柄（真实 ABI 是指针/数组；fake_libc typedef 成 int 会被内建覆盖，尺寸按 8B 更贴近现实）

**调用层**（execute.py 拦截 + cbuiltins 帧栈）：
- `__builtin_va_start(ap, last)` / `__builtin_va_arg(ap)` / `__builtin_va_end(ap)` / `__builtin_va_copy(dst, src)`
- 关键限制：fake_libc 宏 `va_arg(_ap, _type)` 展开为 `__builtin_va_arg((_ap))`——**类型实参被丢弃**，只能透传 Python 值（解释器实参本就是值，无类型转换需求）
- **按调用帧栈管理**：每次 FuncDef 调用压入一帧（`name -> 剩余实参快照`），返回弹帧——内外层同名 `ap` 互不干扰；宏展开后实参是值无法写回变量，故 execute.py 从 AST 实参取首实参 **ID 名**按名操作当前帧
- 可变参数源 = 调用点形参之后的实参列表（`args[len(param_names):]`）
- 解析器限制：裸类型实参（`va_arg(ap, int)` 未宏展开时）pycparser 无法解析（`Invalid expression INT`）——与裸 typedef 名惰性同源，须经宏展开

## 八、测试计划

`test_builtins.py`（6 组）：
1. 注册表机制：list/call/动态注册/装饰器注册/未注册报错
2. stdio：printf 格式转换（含 `%p/%ld/%zu`）、puts、putchar
3. stdlib/string：malloc/memset/memcpy（检查堆字节）、strlen/strcmp/strncmp/atoi/abs/free
4. ctype/math/rand：isdigit/toupper/sqrt/pow/floor、srand 可复现
5. 端到端：C 文件 `malloc+memset+printf+strlen+atoi` → `main()=17`、stdout 断言
6. va_list：`sum(3,10,20,12)=42` + 嵌套调用隔离（`outer` 中途调 `inner`，外层 va 不破坏）→ `main()=42`

## 九、与后续路线衔接

| 后续 | 衔接点 |
|---|---|
| MEM-1 指针模型 | 堆地址与 `*`/`[]` 解引用打通；`char*` 语义完整 |
| MEM-4 堆管理 | bump → first-fit/free 回收；越界更严格 |
| TYPE-2 隐式转换 | printf 参数类型检查可接入 |
| 更多内置 | 按 §三 三步注册即可（如 sprintf/sscanf/getenv） |

---

*基线：lab/learning `4f4bf90`* ｜ *代码：`cbuiltins.py` / `execute.py` ExeFuncCall/ExeConstant*
