---
tags: [pycparser, execute.py, 设计文档, 惰性解析, 运行时绑定]
created: 2026-08-15
---

# 设计文档：惰性解析（跳过 link，运行时按需解析）

> 问题：能否不要 `link` 这一步，改为**运行时按需解析**（类似 Python 的动态绑定）？
> 动机：运行某个函数时，不想把不需要的函数/结构体全部检查/解析一遍。
> 现状基线：lab/learning `83b9320`

---

## 一、现状：link 做了什么，代价是什么

`CProgram` 三段式：`load()`（逐文件 cpp+解析，**只产出 FileAST，不解析语义**）→ `link()` → `run()`。

`link()` 三阶段的实际代价：

| 阶段 | 做什么 | 代价 |
|---|---|---|
| 1a 类型收集 | 全量注册 typedef/struct/union/enum，**解析成员类型与布局** | 任一文件的类型错误（含未使用文件）都阻断整个程序 |
| 1b 函数收集 | 全量注册 FuncDef，**解析函数签名**（参数/返回类型） | 签名里的类型错误同样提前暴露 |
| 2 全局变量 | **执行**全部顶层 Decl 的 init（`int g = helper();` 真的会调 helper） | 全局 init 的副作用/错误在 link 时发生 |
| main 定位 | 只按名字扫描 FuncDef（轻量） | 几乎无 |

**实测验证痛点**：`lazy_good.c`（main 返回 42）+ `lazy_bad.c`（`int arr[no_such_const+1]`）→ `link()` 被 bad 文件阻断，即使 main 从不引用它。且 `run()` 强制要求已 link。

## 二、需求本质：可达性驱动的解析

用户的诉求 = **只解析"运行路径可达"的符号**（函数、类型、全局变量），不可达代码不检查——这正是解释器（相对编译+链接）的核心价值，Python 的体验来源。

**一个重要澄清**：Python 的"惰性"并非全惰性——

- **模块级是 eager 的**：`import` 一个模块会按顺序执行其全部顶层语句（含 def），所以函数注册在 import 时完成；
- **惰性的是**：函数体内部的名字绑定（调用时才查）、类型检查（动态类型，无静态检查）。

因此"Python 式"最贴切的模型是：**文件级 eager（import 时注册该文件全部顶层）+ 跨文件惰性（用到哪个文件才"import"哪个）**。

## 三、可行性分析（按符号类型）

| 符号 | 可行性 | 关键难点 |
|---|---|---|
| **函数调用** | ✅ 完全可行 | 调用点查 `g_functions` 未命中 → 从"源池"找 FuncDef 即时注册 → 再调用。互递归 OK（A 调 B 触发 B 注册，B 调 A 时 A 已注册）；注册幂等（已存在则跳过） |
| **类型引用** | ⚠️ 可行，3 个坑 | ① **完整性**：`struct S s;`（by value）与 `sizeof(struct S)` 要求 S 完整，需在引用点即时找定义并布局；② **自引用**：`struct Node { struct Node *next; }` 需"先注册不完整再补成员"（`_register_compound` 已支持）；③ **环**：文件间类型互引需激活守卫 |
| **全局变量** | ⚠️ 语义冲突 | C 要求全局 init 在 main 之前按定义序执行；全惰性会让 init 乱序/延迟到首次访问（不再"程序启动即初始化"）。**文件级惰性可保留文件内顺序**（见方案 B） |
| **冲突检测（S2）** | ⚠️ 漏检 | 惰性下只有被装载的文件才做冲突检测；未使用文件的 typedef 冲突不报（可接受：Python 同样不检查未 import 的模块） |
| **main 定位** | ✅ 轻量 | 只按名字扫 FuncDef，不解析类型——保留 |
| **报错时机** | 双刃剑 | 提前（link）→ 一次暴露全部错误（C 风格）；延后（惰性）→ 只报运行路径的错误（Python 风格） |

## 四、三种方案对比

### 方案 A：全惰性（函数/类型/全局全部按需解析）

- 函数调用点注册、类型引用点注册、全局首次访问时求值 init。
- ✅ 最"Python"；❌ **全局 init 次序丢失**（`int g = helper();` 何时跑？首次读 g 时？语义漂移）、类型完整性难保证、与 C 语义偏差最大。

### 方案 B：文件级惰性装载（**推荐**，Python import 语义）

- `load()` 建一个轻量**符号索引**：`符号名 → (所在文件, AST 节点)`（只扫 ext 名字，不解析类型——零检查、零错误）。
- 解析钩子（函数调用/类型引用/全局访问）未命中 → 查索引 → **激活（import）其所在文件**：按序执行该文件顶层（该文件的 1a 类型 → 1b 函数 → 2 全局 init），每个文件只激活一次（幂等）。
- 激活环：`激活中` 状态标记；文件激活期间其符号可被部分引用（函数即时注册，类型靠 `_register_compound` 的"不完整→补全"）。
- ✅ 保留文件内 C 语义（全局 init 顺序）；✅ 跨文件惰性（只 import 用到的文件）；✅ 实现简单（复用现有 `execute(ext)` 逐节点逻辑）；❌ 跨文件冲突检测变为"用到才检"。

### 方案 C：可达性预链接（反向收集，保持"先检查后运行"）

- 从入口函数反向收集依赖（函数→函数、函数→类型、类型→文件），只对**可达文件/符号**做三阶段 link。
- ✅ 仍是"运行前检查"，但只检查可达部分；❌ 实现较重（需要依赖图遍历），且不可达代码的错误仍不报（和 B 一样）。

### 对比结论

| 维度 | A 全惰性 | B 文件级惰性 | C 可达性预链接 |
|---|---|---|---|
| 满足"不检查无用代码" | ✅ | ✅ | ✅ |
| 全局 init 次序 | ❌ 乱序 | ✅ 文件内保序 | ✅ |
| 实现复杂度 | 中 | **低** | 高 |
| 与现有 link 兼容 | 需大改 | link 保留为显式选项 | link 保留 |
| C 语义保真度 | 低 | **中高** | 高 |

**推荐：方案 B 为主**（默认惰性，`run()` 内部自动装载）；**方案 C 可选**（`strict=True` 时做可达性预链接，保留"运行前检查"）。现有 `link()` 保留（显式调用即当前行为，测试/需要全量检查时用）。

## 五、推荐方案 B 详细设计

### 5.1 符号索引（SourceIndex）

```python
class SourceIndex:
    """符号 → 定义位置；文件激活状态机。"""
    def __init__(self):
        self.files = {}        # 文件路径 -> FileAST
        self.symbols = {}      # 符号名 -> (文件, 节点)   （类型标签/typedef/函数/全局变量）
        self.state = {}        # 文件 -> 'unloaded' | 'loading' | 'loaded'

    def build(self, asts, files): ...   # 只扫 ext 名字，不解析类型（零检查）
    def locate(self, name): ...         # 查符号所在文件
    def activate(self, path): ...       # 装载一个文件（幂等 + 环守卫）
```

符号分类（同 C 命名空间）：
- 类型：`('struct', tag)` / `('union', tag)` / `('enum', tag)` / typedef 名
- 函数：函数名
- 全局变量：变量名

### 5.2 解析钩子（三处）

```python
# ① 函数调用（ExeFuncCall）
if func_name not in g_functions and not is_builtin(func_name):
    source_index.activate_for(func_name)      # 定位并装载定义文件
    # 仍找不到 → AssertionError（真正的未定义）

# ② 类型引用（typesys.resolve / _register_compound 未命中时）
if not g_types.has_tag(kind, tag):
    source_index.activate_for_type(kind, tag)  # 装载定义文件后再查一次

# ③ 全局变量（首次访问/声明时）
if name 未注册且是全局符号:
    source_index.activate_for(name)
```

### 5.3 激活（import）语义

```
activate(path):
    if state[path] == 'loaded': return
    state[path] = 'loading'
    ast = files[path]
    for ext in ast.ext:                     # 文件内按源顺序
        if 是类型定义:  execute(ext)          # 类型先注册（1a 语义）
    for ext in ast.ext:
        if 是 FuncDef:  execute(ext)          # 再函数（1b 语义）
    for ext in ast.ext:
        if 是全局 Decl: execute(ext)          # 最后全局 init（2 语义）
    state[path] = 'loaded'
```

- 激活期间若引用了另一个未装载文件的符号 → 递归 `activate`（DAG，`loading` 标记防环；环内靠函数即时注册/类型不完整补全兜底）。
- **入口调用前**：`activate(入口文件)`（保证 main 所在文件已装载，其全局已初始化），随后 `FuncCall main`。

### 5.4 对现有功能的影响

| 现有能力 | 惰性模式下的变化 |
|---|---|
| S2 冲突检测 | 只对已装载文件生效（未使用文件的冲突不报）；`strict` 时可用方案 C 全量检查 |
| 前向引用（T2） | 天然支持（调用点即时注册），且**不再依赖文件顺序**（比现在更强） |
| 全局变量 init（T4） | 在所属文件首次被用时初始化（比"link 时全部执行"更 Python） |
| 报错信息 | 从"link 期全量"变为"运行路径上的首个错误"（更接近解释器体验） |
| 既有测试 | `link()` 保留，现有测试不变；新增惰性模式测试 |

## 六、实施计划

| 阶段 | 内容 | 验收 |
|---|---|---|
| **L1**（1 天）✅ 已完成 | `SourceIndex` + `activate`（文件级三阶段）+ 函数调用钩子 | ✅ 惰性运行双文件（跨文件函数无需 link）；坏文件不影响好文件；前向引用与文件顺序无关；全局变量在所属文件激活时初始化 |
| **L2**（1 天）✅ 已完成 | 类型引用钩子（IdentifierType + struct/union/enum 引用）+ 环守卫 | ✅ `struct Pt` / `enum EColor` 跨文件惰性解析（main 不定义、运行中按需装载）；真正未定义仍报错 |
| L3（1 天） | `run(lazy=True)` 默认；`link()` 保留；冲突检测适配 | T1-T8 在惰性模式下全过；`strict` 走方案 C（可选） |

**L1 实现注记**：
- 新增 `sources.py`：`SourceIndex`（零检查符号索引：函数/标签/typedef/全局变量 → 定义文件）+ `activate(path)`（文件内 1a 类型 → 1b 函数 → 2 全局，幂等 + `loading` 环守卫）+ 全局单例 `g_source_index`
- `execute.py` `ExeFuncCall`：未注册且非内置 → `g_source_index.activate_for(name)` 按需激活（幂等 no-op）
- `program.py`：`CProgram(..., lazy=True)` —— `load()` 建索引，`run()` 自动 `activate(入口文件)`；`link()` 保留（lazy=False 默认，零破坏）
- 测试：`test/multi/lazy_{a,b,bad}.c` + test_program 新增 3 组 L1（坏文件不激活/顺序无关/全局激活时初始化）
- 顺带效果：**前向引用不再依赖文件顺序**（调用点即时注册，比 link 更强）

**L2 实现注记（含 3 个实战发现）**：
- `typesys.py`：`_resolve_type_lazy`（IdentifierType 未定义 → 激活后重试）+ `_register_compound`/`_enum_from_node` 引用惰性钩子（struct/union/enum 引用未注册标签 → 激活定义文件）
- `sources.py` `build()`：补索引**被 TypeDecl/Decl(name=None) 包装的内嵌标签**（`typedef struct {...} S;` 的标签、裸 `enum E {...};`）——否则 `activate_for_type` 找不到定义
- `execute.py`：新增 `g_global_scope`（根作用域）；**文件激活时顶层在全局作用域执行**（激活可能发生在函数调用中途，全局变量/枚举常量必须落在全局，否则随函数返回丢失）
- `ExeDecl` 裸类型定义分支：enum 定义走 `_process_enum_node`（注册 + 注入常量）
- **解析器限制（重要）**：裸 typedef 名跨文件使用（`Mid m;` 而 Mid 定义在别处）**无法解析**——pycparser 只有本文件先见 typedef 才把名字当类型（TYPEID），解析先于惰性逻辑。**惰性类型解析仅对带关键字的 struct/union/enum 引用生效**；typedef 名惰性需解析器预置 TYPEID（不在本方案范围）
- 测试：`test/multi/lazy_{t,t_main,e,e_main,u_main}.c` + 3 组 L2（struct/enum 跨文件、真正未定义报错）；夹具命名需**唯一**（g_types 跨测试持久，`enum Color`/`struct Pt` 会与 e2e/T3 夹具冲突）

## 七、结论

**可以，且推荐**。但不应做"全惰性"（A 会破坏 C 的全局初始化次序），而应做**文件级惰性装载（方案 B，Python import 语义）**：`load()` 只建零检查的符号索引，运行中首次用到某文件才激活其顶层定义（类型→函数→全局，文件内保序、跨文件幂等）。这样"运行 main 时，不碰 helper 所在文件就不会检查它"——达成用户目标，同时保留 C 语义的关键部分；`link()` 保留为显式全量检查选项，现有测试零破坏。

---

*基线：lab/learning `83b9320`* ｜ *关联：设计文档-多文件支持.md（link 三阶段）、设计文档-内置函数.md*
