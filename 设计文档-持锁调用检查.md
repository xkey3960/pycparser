# 设计文档-持锁调用检查（LOCK-CHECK）

> 对应笔记《持锁调用静态分析.md》（13.技术文档/静态分析）讨论的落地实现。
> 需求：静态分析出所有"在持有锁的状态下调用函数 F"的违规——规则是
> **任意加锁动作之后、解锁之前，都不允许调用 F**（不区分是哪把锁）。
> 目标：gcc -E + GnuCParser 纯静态分析（GNU 扩展可解析），
> **不依赖解释器**（execute.py/typesys 全局状态一律不碰）。

---

## 一、规则形式化

数据流问题：状态 = 当前持有的**锁集合** L

```
加锁语句:  L := L ∪ {lock}
解锁语句:  L := L \ {lock}
调用 F 且 L ≠ ∅  →  违规
```

- 锁身份：调用参数表达式的**规范化字符串**（如 `&g_ledger.lock`、
  `&from->lock`）——同一把锁在同一程序内键稳定。
- 规则约束"任何锁"，比特定锁配对检查简单：报告条件是 `L ≠ ∅`。

## 二、解析（自包含，不依赖 CProgram 内部）

`gcc -E`（子进程）+ `GnuCParser.parse(text, path)`，自包含实现：

- **UTF-8 解码 + 换行归一**：Windows GBK 系统下 pycparser 原管线
  （`check_output(universal_newlines=True)`）按 GBK 解码，UTF-8 源文件
  必崩；改为按 UTF-8（errors=replace）解码并归一 CRLF。
- **#line 保真**：`#line` 里的文件名是 GBK 字节，与 UTF-8 内容混流无法
  单编码解码——保留 `#line`（行号真实），只把解码损坏（含 U+FFFD）的
  引号文件名替换为 ASCII 占位 `"src"`，行号与源码一一对应。
- **每文件重试 3 次**：云盘占位/同步中的瞬时空文件会解析出
  "At end of input"，重试等待内容落盘。
- **线程池并行**：与 QOL-4 同思路（多文件并行 -79%）。

## 三、分析设计

### 总体架构

```
lockcheck.py
 ├─ ① 解析：gcc -E（UTF-8 + 重试）+ GnuCParser → FileAST[]
 ├─ ② 函数收集：FuncDef 名 → (节点, 文件)；已知函数集（FuncDef +
 │    函数声明 + 内置库函数）——未知名字的调用 = 函数指针告警
 ├─ ③ CFG 构建：每个函数 AST → 基本块图（含表达式内调用事件）
 ├─ ④ 函数内分析：前向 may-数据流（并集合并，块级不动点）
 └─ ⑤ 跨函数：callee 无关摘要 + IN 单调传播（三阶段，无振荡）
```

### 事件模型

每个语句/表达式中出现的 `FuncCall` 按求值顺序抽取为事件：

| 事件 | 触发 | 载荷 |
|---|---|---|
| `acquire` | 调用名 ∈ acquire 原语表 | 锁身份、coord |
| `release` | 调用名 ∈ release 原语表 | 锁身份、coord |
| `forbidden` | 调用名 ∈ 禁调表 | coord |
| `call` | 其它普通调用 | 被调名、coord、调用点状态 |
| `indirect` | 函数指针调用（名字非 ID） | coord（告警，不静默） |

**表达式内调用**必须处理：`if (mutex_lock(&m) == 0)` 的加锁发生在条件
表达式里——所有表达式（cond/init/next/赋值右值/return/实参）统一走
`expr_calls()` 抽取。

### CFG 构建

语句级线性化 + 结构化控制流（两遍：先编号+收集标签，再建边）：

- `If`：条件节点 → 两分支；`While/DoWhile/For`：条件/体/步进节点 +
  回边（break→出口、continue→回边）
- `Switch`：条件节点 → 各 Case/Default；Case 之间 C 语义**落穿**
- `Goto`：跳转目标标签节点（标签两遍收集）；`Return`：出口节点
- 每个语句节点挂自己的事件列表

### 函数内分析（may-分析）

```
IN[entry] = 调用者传入的持锁集合（入口参数化）
块级 worklist：状态单调并集合并 → 终止（有限块 + 有限锁集合）
forbidden 事件且状态非空 → 记录违规(函数, coord, 持锁及获取点)
```

### 跨函数摘要（三阶段，callee 无关，无振荡）

初版把 adds/rels 与 IN 放在同一个不动点里迭代，**振荡不收敛**：
`adds[lock_ledger]` 需要调用者不持锁、`rels[unlock_ledger]` 需要调用者
持锁——两个摘要互相矛盾，在两组值间来回跳。修复：**摘要与调用者解耦**。

| 阶段 | 内容 | 收敛性 |
|---|---|---|
| A. callee 无关摘要 | `adds[G]` = entry=∅ 分析下的出口持锁（G 净新增）；`rels[G]` = G 函数体内释放事件命中的锁集合（**语法计算**，与调用者无关） | 单调（adds 随 callee adds 增长） |
| B. IN 传播 | 用固定摘要，函数内状态只随 IN 单调增长；调用点 `S' = (S \ (rels[H] ∩ S)) ∪ adds[H]`，`IN[H] ∪= S`（只存锁 id） | 单调（IN ⊆ 有限锁集） |
| C. 收集 | 稳定 IN + 稳定摘要下收集违规/间接调用；按违规点去重，持锁并集 | — |

**wrapper 天然覆盖**：`lock_ledger() { mutex_lock(&g); }` 不配表——
阶段 A 得 `adds = {&g_ledger.lock}`，调用者自动看到锁被持有；
`unlock_ledger` 的 `rels`（语法释放集）让调用者移除锁。
条件释放（同一锁仅某分支解锁）会保守移除 → 可能漏报，已在 §四 标注。

## 四、精度与 soundness 边界

- **may-分析 + 上下文合并（IN 取并集）**：模型内**无漏报**；误报来源 =
  同一函数"持锁调用"与"不持锁调用"两上下文被合并。
- **函数指针**：间接调用不解析目标，输出 `indirect` 告警（标注不可见
  调用目标，人工补位）——**不假装 sound**（对应笔记 §8.4）。
- **锁身份规范化**：参数表达式递归字符串化（`&`、`.`、`->`、`[]`、
  强转、常量），别名（指针传参同一把锁不同表达式）会分裂成不同键 →
  保守处理（宁可多报）。
- **条件释放（已知近似）**：`rels` 是"函数体释放事件命中"的语法集合，
  若同一锁仅某分支释放（调用者路径上可能未释放）→ 保守移除该锁 →
  可能漏报；demo 中均为无条件释放，无此问题。

## 五、CLI

```
python lockcheck.py <a.c> [b.c ...] \
    --acquire pthread_mutex_lock,mutex_lock,spin_lock \
    --release pthread_mutex_unlock,mutex_unlock,spin_unlock \
    --forbid log_tx,log_event \
    [--json] [--quiet]
```

默认 acquire/release 含 pthread/spin/mutex 常见名；`--forbid` 必填。
输出：每处违规 = 文件:行 + 函数 + 禁调名 + 持锁列表（含获取位置）；
结尾汇总违规数、间接调用告警数。

## 六、测试工程与用例设计

`examples/lockcheck_demo/`：多文件银行转账系统（ledger 全局锁 +
account 每账户锁 + logger 禁调函数 + worker 线程 + main 函数指针）。
埋点矩阵（test_lockcheck.py 断言）：

| 用例 | 形态 | 期望 |
|---|---|---|
| 直接违规 | 加锁→调 forbidden→解锁 | 报 |
| 跨函数违规 | 持锁调 helper，helper 内调 forbidden | 报（helper 处，锁溯源到调用者） |
| 循环内违规 | for 内加锁→forbidden | 报 |
| 分支内违规 | 仅 err 分支加锁→forbidden | 报 |
| callee 内部加锁 | 被调函数内加锁→forbidden（调用者无锁） | 报 |
| wrapper 传播 | lock_ledger 包 mutex_lock，调用者持锁可见 | 报 |
| 干净路径 | 加锁→解锁→再调 forbidden | 不报 |
| 无锁调用 | 不持锁调 helper（helper 内调 forbidden） | 不报 |
| 间接调用 | 函数指针调 forbidden | 告警（不报违规） |

## 七、落地状态（2026-08-29 完成 ✅）

1. `lab/lock-check` 分支：设计文档 → lockcheck.py → demo 工程 → test_lockcheck.py
2. 验证：
   - demo 全部 5 处埋点命中、干净点零误报、间接调用告警 1 处；
     违规行号与源码一一对应（#line 保真）
   - `test_lockcheck.py` 6 组断言全绿；既有 pytest **252 passed, 6 skipped** 无回归
   - CLI 退出码：0 无违规 / 1 有违规（可接 CI）；`--json` 结构化输出
