# 设计文档-性能优化（QOL-3）

> 对应 TODO.txt `[QOL-3] 性能优化`（P4 远期，TODO 表剩余唯一项）。
> 目标：在不改变语义、不破坏既有测试（test_typesys/test_program/test_exe/
> test_builtins/test_qol2 + 上游 pytest）的前提下，降低解释器热路径开销。

---

## 一、现状

`execute(node)` 是唯一分发入口（execute.py:416），每次求值都经过：

```python
def execute(node):
    if node is None:
        return None
    class_name = node.__class__.__name__        # ① 每次取类名字符串
    if class_name in g_exe_class:               # ② 字符串 → 字典查找
        try:
            return g_exe_class[class_name](node).execute()   # ③ 构造执行器对象
        except InterpreterError as e:           # ④ 两层异常捕获
            ...
        except (TypeError, IndexError, ...) as e:
            ...
    raise AssertionError(...)
```

### 三个已知开销点（TODO 原文）

| # | 开销 | 位置 | 影响 |
|---|------|------|------|
| 1 | `g_scope` 全局状态反复切换 | ExeCompound/ExeFor/ExeFuncCall 每次进入/退出块或调用都 `g_scope = Scope(outer)` + finally 恢复 | 块级作用域每层分配新 Scope 对象 |
| 2 | 字符串匹配查找 | `execute()` 每次 `node.__class__.__name__` + 两次 dict 查找 | 每个节点 2 次字符串操作 |
| 3 | 异常控制流 | Break/Continue/Return 靠异常抛接 | 循环每轮 try/except，深调用栈抛接 ReturnException |

### 实测依据（热循环示例）

```c
int main(void){ int s=0; for(int i=0;i<100000;i++) s+=i; return s; }
```

- 每次 `s+=i`：execute(BinaryOp) → execute(ID s) ×2 + execute(ID i) + execute(Constant 1)
  - 每个 ID/Constant 都要：取类名 → 查表 → 构造对象 → 调 execute() →（返回）
- 每轮循环：execute(For) → execute(cond) → execute(Compound) → 新建 Scope → execute(stmt)
  - 还伴随 BreakException 的 try/except 建立

---

## 二、设计

### 方案总览：三层优化，各自独立、可叠加

```
A. 缓存分发（去掉字符串查找）      —— 低风险，最通用
B. 简单节点内联（跳过执行器对象）   —— 中风险，覆盖最大热点
C. 循环控制流去异常化（可选/保守）  —— 高风险，改动循环语义风险大
```

### A. 缓存分发：类 → 执行器类，一次性映射

现状每次 `class_name = node.__class__.__name__` 生成字符串再查表。
改为**类对象直接作 key**：

```python
# 模块加载时构建：类 → 执行器类
_EXE_BY_CLASS = {cls: exe_cls for cls, exe_cls in
                 ((getattr(c_ast, k), v) for k, v in g_exe_class.items())
                 if hasattr(c_ast, k)}

def execute(node):
    if node is None:
        return None
    cls = node.__class__
    exe_cls = _EXE_BY_CLASS.get(cls)
    if exe_cls is not None:
        try:
            return exe_cls(node).execute()
        except InterpreterError as e:
            _attach_coord(e, node)
            raise
        except (TypeError, IndexError, ZeroDivisionError, KeyError,
                AttributeError, ValueError, OverflowError) as e:
            ...  # QOL-2 包装逻辑不变
    raise AssertionError(f"Unexpected AST node: '{cls.__name__}'")
```

- `g_exe_class` 保留（外部/测试可能引用），`_EXE_BY_CLASS` 是其类键镜像。
- 兜底分支仍用 `cls.__name__` 生成错误消息（仅在未知节点时执行，非热路径）。
- GNU 扩展节点（TypeDeclExt/StructExt 等）同样有对应类 → 自动覆盖。

### B. 简单节点内联：Constant / ID 直接求值

现状：`execute(Constant)` → 构造 ExeConstant → 调 .execute() → 方法内 dict 查找。
热点是**常量与变量读取**（表达式树中占比最高），改为在 `execute()` 顶层特判：

```python
def execute(node):
    if node is None:
        return None
    cls = node.__class__
    if cls is c_ast.Constant:
        return _exec_constant_inline(node)
    if cls is c_ast.ID:
        return _exec_id_inline(node)
    exe_cls = _EXE_BY_CLASS.get(cls)
    ...
```

`_exec_constant_inline`：把 ExeConstant.execute 的正文提为模块级函数
（`self.node` → 参数 `node`），逻辑不变。
`_exec_id_inline`：把 ExeID.execute 正文提为模块级函数（引用 `g_scope`/
`g_static_globals`/`g_static_funcs`/`g_functions`/`g_call_stack`/`g_current_file`）。

> 注意：**GNU 扩展节点也有 Constant/ID 子类吗？** pycparserext 不新增
> Constant/ID 子类（它们是 c_ast 基类节点），`is` 判断安全。
> 但若未来有子类化（如 TypeOfExpression 内含 ID），`cls is c_ast.Constant`
> 精确匹配仍正确——子类走普通分发。

### C. 循环控制流去异常化（保守项，默认不做）

TODO 原文建议 while 的 Break 改返回值标记。风险：
- Break/Continue 需贯穿多层嵌套块（Compound 内层 break 要传到外层循环），
  返回值标记要穿透每层 Compound/If/Switch，改动面大、易引入语义回归。
- ReturnException 深栈抛接（函数调用链）已由 MEM-2 栈帧承担，异常路径
  只在真正 return 时触发一次，非循环热路径。

**结论**：C 项本期不做，仅记录。A+B 已覆盖"字符串查找 + 对象构造"两个
主要热点；g_scope 切换与异常抛接属于语义必需（块作用域/控制流），保留。

---

## 三、影响与兼容性

| 关注点 | 结论 |
|--------|------|
| 语义 | A/B 均为纯重构（提函数/换查找方式），求值结果逐位不变 |
| 错误体系 | QOL-1 coord 附着、QOL-2 裸异常包装逻辑原样保留（内联分支也走同一包装？→ **否**：内联分支直接抛 InterpreterError，经顶层 except 捕获附着 coord） |
| 测试兼容 | 既有 `except AssertionError` 测试零破坏；上游 pytest 用 GnuCParser 解析，A/B 对解析无影响 |
| g_exe_class | 保留原字典（外部引用兼容），新增 `_EXE_BY_CLASS` 镜像 |
| 调试 | 未知节点错误消息仍含类名（用 `cls.__name__`，非热路径） |

### 内联分支的异常处理细节

内联 `_exec_id_inline` 抛 `UndefinedVariable`（InterpreterError 子类）时，
经 `execute()` 顶层 `except InterpreterError` → `_attach_coord` 附着位置，
与原 ExeID 路径完全一致。常量解析不会抛解释器错误（失败宽松返回原值），
故内联分支无需裸异常包装。

---

## 四、实施步骤

1. 构建 `_EXE_BY_CLASS` 类键镜像（`g_exe_class` 之后定义，遍历生成）。
2. 把 `ExeConstant.execute` 与 `ExeID.execute` 正文提为模块级函数
   `_exec_constant_inline(node)` / `_exec_id_inline(node)`（原类方法委托调用，
   保持类结构完整便于回归对照）。
3. `execute()` 分发改为：`cls is c_ast.Constant` / `cls is c_ast.ID` 特判 →
   `_EXE_BY_CLASS.get(cls)` 查表 → 原异常处理不变。
4. 新增基准脚本（临时，不入库）：热循环 `for i<1e6 s+=i` 与递归 fib(20)，
   对比优化前后耗时（同机同解释器）。
5. 全量回归：test_typesys / test_program / test_exe / test_builtins /
   test_qol2 / 上游 pytest 130 passed。
6. TODO.txt：QOL-3 标记 ✅，优先级表更新为"全部完成"。

---

## 五、验收标准

- [ ] 全量回归通过（6 套本地测试 + 上游 pytest 130 passed, 6 skipped）
- [ ] 热循环基准：A+B 后耗时 ≤ 优化前 70%（预期 40-70%，不设硬指标）
- [ ] 语义抽查：fib(20)/三层循环/多维数组结果与优化前一致
- [ ] 错误路径抽查：未定义变量/越界/除零仍报 InterpreterError 子类 + coord
- [ ] GNU 扩展抽查：语句表达式/typeof/attribute/container_of 不回归
