---
tags: [pycparser, cbuiltins.py, 设计文档, 内存模型, 堆]
created: 2026-08-20
---

# 设计文档：堆与 malloc/free（MEM-4）

> 问题：当前 malloc 是 **bump 分配器**（`_HEAP_NEXT` 单调递增），free 只移除
> 分配记录不回收空间——反复 malloc/free 后堆耗尽。需 first-fit + 空闲块复用。
> 现状基线：lab/learning `c5571a2`（MEM-1 Address 指针模型已完成）

---

## 一、现状：bump 分配器的局限

```python
_HEAP = bytearray(1024 * 1024)   # 1MB 字节堆
_HEAP_NEXT = 0                   # bump 游标：只增不减
_ALLOCS = {}                     # addr -> size（free/realloc 校验）

def _alloc(size):
    # 直接从游标切一块；游标前移
    addr = _HEAP_NEXT
    _HEAP_NEXT += size
    _ALLOCS[addr] = size
    return addr

def _free(addr):                 # 现状：仅移除记录，空间不回收
    del _ALLOCS[addr]
```

**问题**：`malloc(100)` → `free` → `malloc(100)` 无限循环后，bump 游标一路涨到
堆尾 → 报"堆空间不足"。典型场景：循环里反复分配释放（链表节点、临时缓冲）。

## 二、设计：first-fit 分配器 + 空闲块表

### 2.1 空闲块表（free list）

```python
_HEAP = bytearray(1024 * 1024)
_ALLOCS = {}          # addr -> size（已分配块；free/realloc 校验）
_FREE = []            # [(addr, size), ...] 空闲块列表（按 addr 升序，便于合并）
```

- `_alloc(size)`：在 `_FREE` 里找 **first-fit**（首个 size>=所需 的块）：
  - 找到 → 从块头切出 size，剩余部分（若 >=8B 最小块）留在 `_FREE`
  - 未找到 → bump 分配（游标后移，追加空闲区）
- `_free(addr)`：从 `_ALLOCS` 取 size → 把 `(addr, size)` 加入 `_FREE` → **合并相邻空闲块**
- 空闲块**元数据外置**（Python 列表管理），不占用堆字节——与真实 malloc 的
  header 内嵌不同，但解释器语义等价且更简单

### 2.2 块合并（coalescing）

free 后尝试与**相邻空闲块**合并（`addr+size == 下一块 addr` 或 `addr == 上一块结束`）：
- 减少碎片，保证大块连续可用
- 实现：`_FREE` 保持按 addr 排序，free 时插入并扫描前后合并

### 2.3 边界与越界检查（保留现状）

- `_read_bytes`/`_write_bytes` 越界抛错（现有）
- `free(非法地址)`：不在 `_ALLOCS` → 报错（double-free / 未分配指针）
- `malloc(0)`/负数：返回 0（NULL 语义，现状保留）

### 2.4 与 MEM-1 Address 的关系

- malloc 返回 **int 地址**（堆内偏移），`_deref(int)` 读字节——现状保留
- first-fit 复用的地址**可能不等于上次 free 的地址**（语义正确：C 不保证）
- 堆不参与 Address 的 loc 定位（Address 定位 scope/list/member；堆是独立线性区）

## 三、对现有功能的影响

| 现有能力 | 变化 |
|---|---|
| malloc/memset/memcpy/strlen（E2E 测试） | 不变（首次分配 bump，行为一致） |
| free 返回值 | 不变（移除记录 + 回收空间） |
| realloc/calloc | 沿用 `_ALLOCS` 校验，配合新分配器 |
| printf/va_list/指针 | 不变（堆地址仍是 int） |
| 越界检查 | 不变（读写边界校验保留） |

## 四、实施步骤

1. `cbuiltins.py`：新增 `_FREE` 空闲块表 + `_alloc` 改 first-fit + `_free` 回收合并
2. `realloc`：用新分配器（旧块 copy 到新块，free 旧块）
3. 测试：
   - 循环 `malloc(100)/free` × 1000 → 不耗尽（bump 会耗尽）
   - 碎片场景：分配大/中/小 → free 中间 → 再分配大块（跨碎片）
   - 相邻块合并：free 两块相邻 → 再分配更大块
   - `free(非法地址)` 报错、`malloc(0)=0`、越界读写报错（回归）
4. 全量回归 + 文档更新

## 五、验收

- ✅ 循环分配/释放不耗尽堆（bump 的缺陷修复）
- ✅ 相邻空闲块合并（碎片减少）
- ✅ free 非法地址报错；malloc(0)=NULL；越界检查保留
- ✅ 现有 malloc/memset/memcpy E2E 零回归

---

*基线：lab/learning `c5571a2`* ｜ *关联：设计文档-内置函数.md（堆现状 §四）、设计文档-指针与地址模型.md（MEM-1）、TODO.txt MEM-4*
