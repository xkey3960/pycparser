# 测试说明（pycparser fork 统一 pytest）

> 本文档说明本 fork（lab/learning）的测试组织方式。
> **核心：仓库根执行 `pytest` 一条命令跑完全部测试。**

## 一、一键跑测试

```bash
pytest                      # 全部测试（本地解释器 + 上游官方）
pytest test_program.py      # 只跑某个文件
pytest -k "mem or type"     # 按关键字过滤
```

配置见 `pytest.ini`（testpaths / addopts）与 `conftest.py`（自动重置）。

### 关键：conftest.py 的自动隔离

`conftest.py` 提供 autouse fixture，**每个测试前**自动：

1. `execute.setup_global_scope()` —— 重置 g_scope / g_functions / 调用栈 /
   static 表 / jmp 缓冲；
2. `g_types.tags.clear()` + `g_types.aliases.clear()` —— 清空类型注册表的
   用户定义（保留内建），**解决跨文件/跨测试同名标签冲突**（如 `struct S`、
   `struct Node` 在多文件复用）。

> 因此测试**不再依赖"夹具命名全局唯一"**——各测试真正隔离。
> 注意：`setup_global_scope` 本身不清 g_types（设计如此），隔离由 conftest 补齐。

## 二、测试文件清单

| 文件 | 覆盖 | 组数 |
|------|------|------|
| `test_typesys.py` | 类型系统 M0-M5 + 修复回归 | ~40 |
| `test_program.py` | S1-S3 + 修复 + L1-L4 + 指针/函数指针/栈帧/类型转换/常量折叠/错误回溯/container_of/static/StaticAssert/方案C/Goto/setjmp | 26 |
| `test_builtins.py` | 内置函数注册表 + stdio/string/heap/MEM-4/ctype/math/rand/va_list | 8 |
| `test_qol2.py` | QOL-2 覆盖率：边界/错误路径/复杂嵌套/GNU 扩展 | 16 |
| `test_qol4.py` | QOL-4 编译管线：并行一致性/缓存命中-失效/开关/缓存可删/组合 | 5 |
| `test_exe.py` | 端到端 test/main.c（main()=165） | 1 |
| `test_ext.py` | GNU 扩展解析 + 函数收集（CustomerCParser） | 1 |
| `test_print_pointer.py` | 链式指针端到端（&A → &pc->stAAA → (AAA*)pc） | 1 |
| `tests/` | 上游 pycparser 官方测试 | 130 passed, 6 skipped |

**当前总计：252 passed, 6 skipped**（本地 ~122 + 上游 130）。

## 三、测试风格约定

- 每个测试是**独立函数**（`def test_xxx():`），pytest 自动收集；
  函数内部 `assert` 判定，`print` 汇报进度（`-s` 显示）。
- 夹具文件（`.c`）放 `test/multi/`，命名 `_前缀_描述.c`（临时）或
  `功能_描述.c`（长期复用）。
- 运行 C 程序的公共入口 `_run(files, entry, args, **kw)`：
  ```python
  exe_mod.setup_global_scope()
  prog = CProgram(files, entry=entry, **kw)
  prog.load()
  prog.link()          # lazy=False 时
  return prog.run(*args)
  ```
- **错误路径测试**：用 `try/except` 断言抛 `InterpreterError` 子类
  （`UndefinedVariable`/`TypeError_`/`MemoryError_`）并检查消息含 coord；
  裸 Python 异常（TypeError/IndexError/ZeroDivisionError）已被 QOL-2
  包装进解释器体系，测试断言分类而非异常类型。

## 四、为什么从"脚本式"改成 pytest

| 旧方式 | 新方式 |
|--------|--------|
| `python test_x.py` 逐个跑（main() 顺序） | `pytest` 一个命令全跑 |
| 每个文件独立进程 → g_types 各文件独立 | 同进程 → 靠 conftest 隔离 |
| 脚本式（test_exe/test_ext/...）无法收集 | 已包成 `test_*` 函数 |
| 无统一断言/进度/过滤 | pytest 内置（-k/-x/--tb） |

## 五、常见问题

- **云盘锁目录报错**：`pytest-cache-files-*` Permission denied 是华为云盘
  锁目录噪音，`addopts = -p no:cacheprovider` 已禁用缓存规避；可无视。
- **test_qol4 缓存**：测试用独立缓存目录
  `test/multi/_q4_cache`（env `PYCPARSER_AST_CACHE`），不污染用户
  `~/.cache/pycparser-ast/`；测试结束自动清理。
- **test_ext**：依赖 `test_ext.c` 与 `examples/` 下的访问器示例。
- **test_linux_h.py**：上游遗留（仅常量定义、依赖不存在的 linux/ 目录），
  无 pytest 可收集测试，忽略。
